export const API_URL = window.API_URL || '';
export const DEVICE_ID = window.DEVICE_ID || '';
export const INACTIVITY_TIMEOUT = window.INACTIVITY_TIMEOUT || 60;
export const TIMER = window.TIMER || 5;
